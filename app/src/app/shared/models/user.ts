export interface User {
    username?: string;
    password?: string;
    token?: string;
}

export interface Customer {
    customerId: number
    firstName: string
    lastName: string
    phone: string
    email: string
    username: string
    password: string
    addressId: number
    userId: number
  
  }
